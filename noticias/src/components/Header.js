import React from 'react';

const Header = () => {
    return (
        <nav className="nav-wrapper light-blue darken-3">
            <a href="/" className="brand-logo center">Noticias React API</a>
        </nav>
    );
};

export default Header;